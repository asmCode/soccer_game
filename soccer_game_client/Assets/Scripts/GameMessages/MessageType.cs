public enum MessageType
{
    PlayerMove,
    PlayerAction,
    PlayerSelect,
    Slide,

    BallPosition,
    PlayerPosition,
}

/*
public enum MatchMessageType
{
    PlayerPosition = 1,
    PlayerAction = 2,
    PlayerSelect = 3,

    BallPosition = 3,
    PlayerPosition = 5,
}
*/
